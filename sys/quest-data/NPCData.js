const MAP = require('./set_map.json')
/*

        other symbols
        // http://www.fileformat.info/info/unicode/category/So/list.htm

*/
const qEnemy = [
  '\u2324', '\u25C9', '\u2331', '\u238A', '>))\u25C9>'
]

const qObject = [
  '\u2398', // 0 // warpGate
  '\u2324', // 1 // -^-
  '\u25A8' // 2 // mysteriousObject
]

module.exports = (mode) =>  {
  if (mode === 'new') {

  }
}
